/** Reuse the original, unaltered asset for every placement. */
export function SolarynLogo(variant = 'normal') {
  return `<img class="solaryn-logo logo-${variant}" src="/brand/solaryn-logo.png" alt="SOLARYN — Climate-aware PV intelligence" width="2048" height="682">`;
}

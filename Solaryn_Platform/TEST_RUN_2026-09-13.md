# User-requested test — 13 September 2026

Result: all exercised checks passed. No application code changed during this test.

## Automated integration suite

Command: `python -m pytest tests -q -p no:cacheprovider --basetemp=workspace/test-user-request-20260913a --tb=short` using the parent Python environment.

**10 passed in 6.93 seconds.** One non-blocking Starlette/httpx deprecation warning.

Coverage: authentication, CSRF/logout, organization isolation, roles, numerical model parity, saved-result persistence, source-document storage, evidence review, eligible and stale approval rules, validation, upload limits, login throttling and integrity checks. These tests use isolated temporary databases.

## Live browser checks

Tested `http://127.0.0.1:8765/` in the existing isolated Synthetic QA workspace, using its pre-existing fictional 100 MWp Riyadh project.

| Check | Observed result |
|---|---|
| Sign-in | Successful; correct organization portfolio displayed |
| Base scenario | Candidate B NPV EUR 33,116,504; LCOE EUR 30.25/MWh; eight evidence gaps |
| Downside: +0.50 pp degradation, 5% yield haircut | Candidate B NPV EUR 23,653,586; LCOE EUR 33.37/MWh |
| Displayed scenario inputs | Values remained 0.5 and 5 after calculation; saved assumptions displayed with result |
| Approval attempt with missing evidence | Rejected: “Resolve evidence gaps and create a new run before approval” |
| Reload | Authentication and project persisted; both new runs appeared in decision history as awaiting review |

Two synthetic run snapshots were added to the QA project. No customer inputs or scientific model files were changed. The displayed values validate software behavior on synthetic inputs, not field performance, scientific calibration, bankability or production security. The complete document-upload/reviewer path was exercised by API tests rather than manually in this browser session.
